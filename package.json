{
  "name": "homebridge-llm-control",
  "version": "1.4.0",
  "description": "Homebridge plugin that adds Telegram control for existing accessories plus LLM-powered monitoring, watchdog, and self-healing.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "config.schema.json",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc -p tsconfig.json",
    "check": "npm run lint && npm run typecheck && npm test && npm run pack:check",
    "lint": "eslint 'src/**/*.ts'",
    "pack:check": "npm pack --dry-run",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node --test test/*.test.cjs",
    "prepare": "npm run build"
  },
  "keywords": [
    "homebridge-plugin",
    "homebridge",
    "llm",
    "openai",
    "telegram",
    "monitoring"
  ],
  "author": "Theo",
  "license": "MIT",
  "homepage": "https://theos2node.github.io/homebridge-llm-control/",
  "bugs": {
    "url": "https://github.com/theos2node/homebridge-llm-control/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/theos2node/homebridge-llm-control.git"
  },
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": "^18.15.0 || ^20.7.0 || ^22 || ^24",
    "homebridge": ">=1.8.0"
  },
  "dependencies": {
    "node-cron": "^4.2.1",
    "openai": "^5.15.0",
    "zod": "^3.25.76"
  },
  "peerDependencies": {
    "homebridge": ">=1.8.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "@typescript-eslint/eslint-plugin": "^8.45.0",
    "@typescript-eslint/parser": "^8.45.0",
    "eslint": "^10.8.0",
    "homebridge": "^2.2.1",
    "typescript": "^7.0.2"
  },
  "homebridge": {
    "pluginAlias": "LLMControl",
    "pluginType": "platform",
    "singular": true
  }
}
